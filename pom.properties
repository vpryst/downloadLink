#Checkstyle settings
checkstyle.maxAllowedViolations = 300

#Jacoco coverage settings
jacoco.class.coverageratio.min = 1.0
jacoco.method.coverageratio.min = 0.80
jacoco.line.coverageratio.min = 0.80

#FindBugs settings
#Possible varialble Low, Medium, Hight
findBugs.threshold = Low
#Enable analyses which increase precision and find more bugs, but which may require more memory and take more time to complete.
#min, default, max
findBugs.effort = Default
